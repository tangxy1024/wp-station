apiVersion: v1
kind: Service
metadata:
  name: {{ include "wp-station.fullname" . }}
  labels:
    {{- include "wp-station.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wp-station.selectorLabels" . | nindent 4 }}
