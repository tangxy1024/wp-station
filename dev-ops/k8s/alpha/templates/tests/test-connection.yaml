apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wp-station.fullname" . }}-test-connection"
  labels:
    {{- include "wp-station.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wp-station.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
